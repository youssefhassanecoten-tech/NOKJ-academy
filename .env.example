# Server
NODE_ENV=development
PORT=3000

# CORS (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:8080

# Database (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGUSER=nokj
PGPASSWORD=nokj
PGDATABASE=nokj_academy

# Auth / JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Optional Mongo (uncomment to use MongoDB instead of PostgreSQL)
# MONGO_URL=mongodb://localhost:27017/nokj_academy